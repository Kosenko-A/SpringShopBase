server:
  port: 8088
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shopBase
    username: postgres
    password: postgres
  jpa:
    show-sql: true
  liquibase:
    change-log: classpath:db/liquibase-changeLog.xml